# Variáveis de ambiente para o projeto Editaliza
# Copie este arquivo para .env.local e preencha com seus valores.
# O arquivo .env.local não é enviado para o Git por segurança.

# --- Configuração do Supabase ---
# Você pode encontrar esses valores no painel do seu projeto no Supabase.
# Vá para Project Settings > API.
NEXT_PUBLIC_SUPABASE_URL="SUA_URL_DO_SUPABASE_AQUI"
NEXT_PUBLIC_SUPABASE_ANON_KEY="SUA_CHAVE_ANON_DO_SUPABASE_AQUI"

# --- Configuração de Segurança ---
# Chave secreta para assinar os tokens JWT.
# Use um valor longo e aleatório em produção. Você pode gerar um com: openssl rand -hex 32
JWT_SECRET="coloque-uma-chave-secreta-forte-aqui"
