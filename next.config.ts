import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['exemplo.com', 'link.com', 'totenart.pt', 'www.encorda.com.br', 'midias.correiobraziliense.com.br'],
  },
};

export default nextConfig;
